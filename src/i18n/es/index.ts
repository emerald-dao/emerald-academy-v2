import type { Translation } from '../i18n-types';

const es: Translation = {
	// this is an example Translation, just rename or delete this folder if you want
	HI: 'Hola, vamo Uruguay!'
};

export default es;
